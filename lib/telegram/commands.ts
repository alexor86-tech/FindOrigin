/**
 * Telegram bot commands handlers
 */

import { sendMessage } from './api'

/**
 * Handle /start command
 * @param {number} chatId - Chat ID
 */
export async function handleStartCommand(chatId: number): Promise<void>
{
  const message = `👋 Привет! Я бот FindOrigin.

Я помогаю найти источники информации для текстов или Telegram-постов.

📝 Как использовать:
• Отправьте мне текст с информацией
• Или отправьте ссылку на Telegram-пост (например, t.me/channel/123)

Я проанализирую текст и найду возможные источники этой информации.

Команды:
/help - показать справку`

  await sendMessage(chatId, message)
}

/**
 * Handle /help command
 * @param {number} chatId - Chat ID
 */
export async function handleHelpCommand(chatId: number): Promise<void>
{
  const message = `ℹ️ Справка по использованию бота FindOrigin

🔍 Что делает бот:
Бот анализирует текст или Telegram-пост и ищет источники информации в интернете.

📤 Как отправить запрос:
1. Просто отправьте текст с информацией
2. Или отправьте ссылку на Telegram-пост в формате:
   • t.me/channel/123
   • https://t.me/channel/123

📊 Что бот делает:
• Извлекает ключевые утверждения, даты, числа, имена
• Ищет источники на официальных сайтах, новостных порталах, блогах, исследованиях
• Возвращает список найденных источников

⚠️ Ограничения:
• Бот не может напрямую получать посты из каналов (требуется дополнительная настройка)
• Результаты поиска зависят от доступности информации в интернете

Команды:
/start - начать работу
/help - показать эту справку`

  await sendMessage(chatId, message)
}
